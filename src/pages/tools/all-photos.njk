---
title: All photos
---

{% from "macros/photo.njk" import photo %}

{% from "macros/css.njk" import css %}
{{ css('components/photos', build.env) }}

<h1>{{ title }}</h1>

<ul class="photos">
  {% for item in collections.unique_photos %}
    {% if loop.index < 4 %}
      {{ photo(item, "eager", build.env, true) }}
    {% else %}
      {{ photo(item, "lazy", build.env, true) }}
    {% endif %}
  {% endfor %}
</ul>
