---
layout: false
---
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
    @font-face {
      font-display:fallback;
      font-family:Coniferous;
      font-style:normal;
      font-weight:400;
      src:url(/ui/fonts/Coniferous-Medium.woff2) format("woff2"),
      url(/ui/fonts/Coniferous-Medium.woff) format("woff");
    }
    @font-face {
      font-display:fallback;
      font-family:Coniferous;
      font-style:normal;
      font-weight:200;
      src:url(/ui/fonts/Coniferous-Thin.woff2) format("woff2"),
      url(/ui/fonts/Coniferous-Thin.woff) format("woff");
    }
    * {
      box-sizing: border-box;
    }
    body {
      background-color: white;
      color: #f7f5fa;
    }
    header {
      width: 1200px;
      height: 630px;
      background-color: rgb(41, 41, 41);
      align-items: center;
      display: flex;
      flex-direction: row;
      flex-shrink: 0;
      flex-wrap: wrap;
      gap: 1rem;
      {# padding: 3rem; #}
    }
    .logo {
      width: 100%;
      align-self: end;
      display: flex;
      align-items: flex-end;
      justify-content: space-between;
      padding: 1rem 3rem 3rem;
      background-color: rgb(31, 31, 31);
      border-top: 2px solid #f7f5fa;
    }
    .logo img {
      width: 522px;
      height: 58px;
    }
    .logo p {
      {# display: none; #}
      font-family: Coniferous;
      margin: 0;
      font-size: 2em;
      font-weight: 400;
      padding-right: 2rem;
    }
    </style>
  </head>

  <body>
    <header role="banner" style="">
      <div class="logo">
        <img src="/ui/logo.svg" alt="Nicolas Hoizey photography" width="100" height="100" importance="high" />
        <p>nicolas-hoizey.photo</p>
      </div>
    </header>
  </body>

</html>
