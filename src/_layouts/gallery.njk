---
layout: page
---

{% set subgalleries = collections.travel | subgalleries(page.url) %}
{% if subgalleries | length %}

<h2>Galleries</h2>

<ul>
  {% for item in subgalleries %}
    <li><a href="{{ item.url }}">{{ item.data.title }}</a></li>
  {% endfor %}
</ul>
{% endif %}

{% set photos_here = collections.travel | photos_here(page.url) %}
{% if photos_here | length %}
  <div class="gallery">
    {% for item in photos_here %}
      <figure>
        <a href="{{ item.url }}">
          <img src="{{ item.url }}{{ item.data.photo }}" alt="{{ item.data.title }}" />
          <figcaption>{{ item.data.title }}, {{ item.date | date("Do MMMM YYYY") }}</figcaption>
        </a>
      </figure>
    {% endfor %}
  </div>
{% endif %}

{% set photos_bellow = collections.travel | photos_bellow(page.url) %}
{% if photos_bellow | length %}
  <div class="gallery">
    {% for item in photos_bellow %}
      <figure>
        <a href="{{ item.url }}">
          <img src="{{ item.url }}{{ item.data.photo }}" alt="{{ item.data.title }}" />
          <figcaption>{{ item.data.title }}, {{ item.date | date("Do MMMM YYYY") }}</figcaption>
        </a>
      </figure>
    {% endfor %}
  </div>
{% endif %}
