{% extends "./base.njk" %}

{% block content %}
  <div class="tools">
    <div class="header">
      {% include 'components/breadcrumb.njk' %}
      <h1>{{ title }}</h1>
    </div>

    {{ content | safe }}
{% endblock %}
