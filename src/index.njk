---
title: Welcome!
---

<h2>Welcome to my photography portfolio!</h2>
<p>Here are the latest photos published here:</p>

<div class="gallery">
  {% for item in collections.photos | limit(6) %}
    <figure>
      <a href="{{ item.url }}">
        <img src="{{ item.url }}{{ item.data.photo }}" alt="{{ item.data.title}}" />
        <figcaption>{{ item.data.title }}</figcaption>
      </a>
    </figure>
  {% endfor %}
</div>
