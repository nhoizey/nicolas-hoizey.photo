---
title: Welcome!
---

<p>Welcome to Nicolas Hoizey photography portfolio</p>

<div class="gallery">
  {% for item in collections.photos | limit(6) %}
    <figure>
      <a href="{{ item.url }}">
        <img src="{{ item.url }}{{ item.data.photo }}" alt="{{ item.data.title | safe}}" />
        <figcaption>{{ item.data.title | safe }}, {{ item.date | date("Do MMMM YYYY") }}</figcaption>
      </a>
    </figure>
  {% endfor %}
</div>
