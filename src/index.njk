---
layout: false
---

{% extends "./_layouts/base.njk" %}

{% from "macros/css.njk" import css %}

{% block content %}
  {% set breadcrumb = [] %}
  {% include 'components/json-ld/breadcrumb.njk' %}

  <section class="welcome">
    <h2 class="page__title">Welcome to my photography portfolio!</h2>

    <p>I’m a French self-taught amateur photographer. I love to <a href="/galleries/travels/">travel</a> to discover new horizons and people, shooting both <a href="/galleries/landscapes/">nature</a> and <a href="/galleries/urban/">urban</a> landscapes, and many other topics.</p>
  </section>

  <section class="topics">
    {% include 'components/sub-galleries.njk' %}
  </section>

  {{ css('components/gallery', build.env) }}

  <section class="latest">
    <h3>Here are the photos I shared the most recently</h3>
    {% from "macros/photo.njk" import photo %}
    <ul class="gallery">
      {% for item in collections.photos_in_galleries | photos_here_and_below('/galleries/') | limit(6) %}
        <li>{{ photo(item, "lazy", build.env) }}</li>
      {% endfor %}
    </ul>
    <p>You can subscribe to <a href="/feeds/">the feed</a> to not miss any new photo.</p>
  </section>

  {{ css('components/blog', build.env) }}

  <section>
    <h3>Here are the latest posts from the blog</h3>
    {% from "macros/meta.njk" import meta %}
    <ol class="blog">
      {% for entry in collections.blog  | limit(3) %}
        <li class="blog__item">
          <h4 class="blog__title"><a href="{{ entry.url }}">{{ entry.data.title }}</a></h4>
          {{ meta(entry) }}
          <p class="blog__text">{{ entry.templateContent | excerpt | striptags | safe }}</p>
        </li>
      {% endfor %}
    </ol>
    <p>You can subscribe to <a href="/feeds/">the feed</a> to not miss any new post.</p>
  </section>
{% endblock %}
